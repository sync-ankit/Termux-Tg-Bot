# Commands to Run the Telegram Bot in terminal 
```
git clone https://github.com/sync-ankit/Termux-Tg-Bot
```
```
cd Termux-Tg-Bot
```
```
bash bot.sh
```

# After that use command help 
4 Options will display select `reset bot` after then put your bot token & Chat id after that `start`
